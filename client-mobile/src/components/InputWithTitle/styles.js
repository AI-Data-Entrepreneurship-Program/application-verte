import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    inputHeader: {
        fontSize: 14
    },
    input: {
        width: 200,
        borderBottomWidth: 1,
        marginBottom: 10
    }
});

export default styles;
