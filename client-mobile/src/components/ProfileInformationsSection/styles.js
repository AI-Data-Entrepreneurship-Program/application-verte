import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        width: '100%',
        alignItems: 'center',
        marginVertical: 10
    },
    avatar: {
        width: 100,
        height: 100,
        borderRadius: 100
    },
    username: {
        fontSize: 21,
        marginTop: 10
    }
});

export default styles;
