import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center'
    },
    exitIcon: {
        margin: 35,
        position: 'absolute',
        bottom: 0
    }
});

export default styles;
